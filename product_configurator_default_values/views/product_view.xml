<?xml version="1.0" encoding="utf-8"?>
<odoo>

   <record id="product_template_form_view" model="ir.ui.view">
        <field name="name">product.configurator.default.product.template.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product_configurator.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='config_line_ids']/tree/field[@name='value_ids']" position="after">
                <field name="default_value_ids"
                       widget="many2many_tags"
                       attrs="{'readonly': [('attribute_line_id','=',False)]}"
                       domain="[('id','in',attr_line_val_ids[0][2])]"
                       options="{'no_create': True, 'no_create_edit': True}"
                       context="{'show_attribute': False}"/>
                <field name="immutable"/>
            </xpath>
        </field>
    </record>

</odoo>
