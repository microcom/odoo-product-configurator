<?xml version="1.0" encoding="utf-8"?>
<odoo>

	<record id="product_template_form_view" model="ir.ui.view">
        <field name="name">product.configurator.product.template.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product_configurator.product_template_form_view"/>
        <field name="arch" type="xml">
        	<xpath expr="//field[@name='config_step_line_ids']/tree/field[@name='attribute_line_ids']" position="after">
        		<field name="restriction_id"/>
        	</xpath>
        </field>
    </record>
    
    <record id="product_config_restriction_form_view" model="ir.ui.view">
        <field name="name">product.configurator.restriction.form</field>
        <field name="model">product.config.restriction</field>
        <field name="arch" type="xml">
            <form string="Configuration Restrictions">
                <group>
                    <group>
                        <field name="name"/>
                    </group>
                </group>
                <notebook>
                    <page string="Rules">
                        <field name="domain_line_ids">
                            <tree editable="bottom">
                                <field name="sequence" widget="handle"/>
                                <field name="attribute_id"/>
                                <field name="condition"/>
                                <field
                                    name="value_ids"
                                    widget="many2many_tags"
                                    options="{'no_create': True, 'no_create_edit': True}"
                                    context="{'show_attribute': False}"
                                    domain="[('attribute_id', '=', attribute_id)]"/>
                                <field name="operator"/>
                            </tree>
                        </field>
                    </page>
                    <page string="Inherited">
                        <field name="implied_ids"/>
                    </page>
                </notebook>
            </form>
        </field>
    </record>
</odoo>